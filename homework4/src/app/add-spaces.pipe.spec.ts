import { AddSpacesPipe } from './add-spaces.pipe';

describe('AddSpacesPipe', () => {
  it('create an instance', () => {
    const pipe = new AddSpacesPipe();
    expect(pipe).toBeTruthy();
  });
});
