import { ThreeSpacePipe } from './three-space.pipe';

describe('ThreeSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new ThreeSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
