import { SpaceThreePipe } from './space-three.pipe';

describe('SpaceThreePipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceThreePipe();
    expect(pipe).toBeTruthy();
  });
});
